---
// SPEC: BRIEF_CLAUDESTACK_PRODUCT.md#1.3.e
// Renders on every /demo route. Sets visitor expectations + drives to /blog.
---

<div class="demo-banner" role="note">
  <span class="demo-banner-label">DEMO CONTENT</span>
  <p>
    These articles showcase the template's capabilities. Written for demonstration purposes only,
    not by the site author.
  </p>
  <a href="/blog">Read real articles by the author →</a>
</div>

<style>
  .demo-banner {
    border: 1px solid var(--green-dim);
    border-left: 3px solid var(--green);
    background: var(--bg-1);
    padding: 14px 18px;
    margin-bottom: 32px;
    font-family: var(--mono);
    font-size: 12px;
    color: var(--text-soft);
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  @media (min-width: 640px) {
    .demo-banner {
      flex-direction: row;
      align-items: center;
      gap: 16px;
    }
  }

  .demo-banner-label {
    flex-shrink: 0;
    color: var(--green);
    font-weight: 700;
    letter-spacing: 0.1em;
    font-size: 11px;
    padding: 2px 8px;
    border: 1px solid var(--green);
    background: var(--bg);
  }

  .demo-banner p {
    margin: 0;
    flex: 1;
  }

  .demo-banner a {
    color: var(--green);
    text-decoration: none;
    font-weight: 600;
    flex-shrink: 0;
    white-space: nowrap;
  }

  .demo-banner a:hover {
    text-decoration: underline;
  }
</style>
